
//直接新建倒计时任务，输入name+添加，输入/选择/追加倒计时时间，天时分秒，勾选配置，可选多次提醒，点击开始。放到倒计时列表。
//倒计时任务结束了，可以放到倒计时列表，可以再次点击启用，就不需要新建。

//直接新建定时任务，输入name，选择时间，勾选配置，勾选是否重复，追加每天/工作日/自定义，工作日可以去设置里面自定义。
//放入定时任务列表

//新建队列任务：
//如多个倒计时，倒计时A25分钟，倒计时B5分钟，类似于番茄闹钟
//倒计时可选开始时间，倒计时开始可以提醒，等于一个定时提醒+一个倒计时提醒


//新建队列任务：
//如单倒计时循环：添加一个倒计时，结束后会自动追加再次倒计时，可设置次数，可设置时间返回如仅在8：00-16：00才执行。
//如多倒计时循环：添加多个倒计时，按照顺序依次执行，全结束后再循环。可设置总次数，也可分别设置单独次数，如倒计时A两次，倒计时B一次，全循环2两次（a,b,a|a,b,a）

//也可以进行更复杂的复合的线性阻塞队列任务，
//如一天的规划
//定时提醒（8：00起床），倒计时任务B（1小时吃饭时间），循环任务C（倒计时任务D，45分钟工作，倒计时任务E，15分钟休息）[循环此时3，时间范围（开始无，结束：12：00中午吃饭）]，
//倒计时任务F,2小时中午休息，再次一个循环任务C2[结束时间为18：00]...
//然后是异步的，不再阻塞任务里的就算是异步的？例如喝水提醒周期任务，但这个任务是一起合并在阻塞任务中，还是独立出来？

//然后如倒计时75%时候进行一个提醒，这时候如果需要延长时间的话，可以点击追加，如在阻塞任务中，可是。

//然后有关于模板设计的，就是可复用的，或是添加/移动，例如把某倒计时任务到阻塞队列中，

//可能还需要时长统计记录。

//    config: { //就两个配置，常驻/静音，
//         requireInteraction: true,  // 是否常驻（覆盖全局配置）
//         silent: false,             // 是否静音（覆盖全局配置）
//     },

//上述的设计可能有些过于复杂了，但我还是想趋向于复杂化进行设计，
//先进行数据结构的设计
//并且这是浏览器拓展的设计，非常规网页，不使用数据库，几乎需要全量读取和覆盖更新
//先进行数据结构的设计，所以怎么设计比较合理并可复用可拓展。
/**
 * 浏览器扩展 - 统一提醒任务数据结构
 * 核心设计：一套结构适配所有类型，嵌套复用，轻量可序列化
 * 存储方式：localStorage 全量存数组，全量覆盖更新
 */
export const REMINDER_TASK_SCHEMA = {
    // ===================== 1. 通用核心字段（所有任务必选）=====================
    id: "task_1743609600_abc123",        // 唯一ID：时间戳+随机数（生成规则固定，无冲突）
    name: "番茄工作法",                   // 任务名称（用户自定义）
    type: "queue",                        // 【核心类型】唯一区分任务类型
    // type枚举：countdown=基础倒计时 | timer=基础定时 | cycle=基础周期 | queue=复合队列
    isTemplate: false,                    // 是否为模板（true=可复用模板，false=运行任务）
    status: "idle",                       // 任务状态（全局统一状态机）
    // status枚举：idle=闲置 | running=运行中 | paused=暂停 | finished=已完成 | disabled=已禁用
    createdAt: "2026-04-02 12:00:00",     // 创建时间
    updatedAt: "2026-04-02 12:00:00",     // 更新时间（自动维护）

    // ===================== 2. 通用配置（所有任务可选，支持继承/覆盖）=====================
    config: {
        requireInteraction: true,           // 通知常驻（覆盖全局配置）
        silent: false,                      // 通知静音（覆盖全局配置）
    },

    // ===================== 3. 通用提醒规则（所有任务都支持，不限倒计时）=====================
    remindRules: [
        // 规则1：进度75%提醒（适配倒计时/周期任务）
        { type: "progress", value: 75, enabled: true },
        // 规则2：剩余10分钟提醒（适配倒计时/定时任务）
        { type: "remaining", value: 600, enabled: true },
        // 规则3：开始时提醒（全局通用）
        { type: "start", enabled: true },
    ],

    // ===================== 4. 类型专属配置（仅对应type存在，无冗余）=====================
    // --------- 4.1 基础倒计时（type=countdown）---------
    countdown: {
        totalSeconds: 1500,                 // 总时长（秒）：25分钟
        // runtime：运行时数据（暂停/恢复必备，不参与模板复用）
        runtime: {
            remainingSeconds: 1500,           // 剩余时长（秒）
            startTime: null,                   // 本次开始时间
            endTime: null,                    // 预计结束时间
        }
    },

    // --------- 4.2 基础定时（type=timer）---------
    timer: {
        targetTime: "2026-04-02 08:00:00",  // 目标触发时间
        repeat: {
            enabled: false,                   // 是否重复
            mode: "workday",                  // 重复模式
            // mode枚举：once=单次 | everyday=每日 | workday=工作日 | weekly=每周 | custom=自定义
            weeks: [1,2,3,4,5],               // 周几重复（0=周日，1-6=周一至周六）
            // 工作日配置：关联全局配置（不重复存储，节省空间）
        }
    },

    // --------- 4.3 基础周期（type=cycle）---------
    cycle: {
        intervalSeconds: 7200,              // 循环间隔（秒）：2小时
        timeRange: {
            start: "08:00",                   // 每日执行开始时间（空=无限制）
            end: "18:00",                     // 每日执行结束时间（空=无限制）
        },
        maxCount: 0,                        // 最大执行次数（0=无限次）
    },

    // --------- 4.4 复合队列（type=queue）【核心：支持阻塞/异步/循环/嵌套】---------
    queue: {
        mode: "sync",                       // 队列模式
        // mode枚举：sync=同步阻塞（顺序执行，核心需求） | async=异步并行（同时运行）
        loop: {
            enabled: true,                    // 是否循环整个队列
            maxCount: 2,                      // 最大循环次数（0=无限次）
            currentCount: 0,                  // 当前已循环次数
        },
        timeRange: {
            start: null,                      // 队列生效开始时间（空=立即）
            end: "2026-04-02 12:00:00",       // 队列强制结束时间（空=无限制）
        },
        tasks: [                            // 子任务列表【完全复用本结构，无限嵌套】
            {
                id: "sub_1",
                name: "专注25分钟",
                type: "countdown",
                status: "idle",
                config: { requireInteraction: true },
                remindRules: [{ type: "progress", value: 75, enabled: true }],
                countdown: { totalSeconds: 1500, runtime: { remainingSeconds: 1500 } }
            },
            {
                id: "sub_2",
                name: "休息5分钟",
                type: "countdown",
                status: "idle",
                countdown: { totalSeconds: 300, runtime: { remainingSeconds: 300 } }
            }
        ],
        currentTaskIndex: 0,                // 当前执行的子任务索引（同步队列必备）
    },

    // ===================== 5. 统计字段（时长统计/执行记录，需求必备）=====================
    statistics: {
        totalExecSeconds: 0,                // 总执行时长（秒）
        completeCount: 0,                   // 完成次数
        lastExecTime: null,                 // 最后执行时间
        totalFinishCount: 0,                 // 总完成次数
    }
};


